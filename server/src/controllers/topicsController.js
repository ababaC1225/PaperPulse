export function getHotTopics(req, res) {
  res.json({ items: [], note: 'Hot topics endpoint placeholder - wire to DB.' })
}

export function getKeywordNetwork(req, res) {
  res.json({ nodes: [], links: [], note: 'Keyword network endpoint placeholder.' })
}
